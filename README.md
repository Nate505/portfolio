## Getting Started

First, run the development server:

```bash
npm run dev
```

The CI/CD pipeline is implemented using GitHub Actions. On each push to the main branch, the pipeline builds a Docker image for the application and pushes it to Docker Hub.
